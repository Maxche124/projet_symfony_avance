{% extends 'base.html.twig' %}

{% block title %}Edit Produit{% endblock %}

{% block body %}

    <h1>{{ 'EditProduit.title'|trans }}</h1>

    {{ include('produit/_form.html.twig', {'button_label': 'Update'|trans}) }}

    <a class="btn btn-sm btn-outline-secondary mt-3"
       href="{{ path('app_produit_index') }}">
        {{ 'backToList'|trans }}
    </a>

    {{ include('produit/_delete_form.html.twig') }}

{% endblock %}